import java.util.Stack;

public class StackDemo {
    public static void main(String[] args) {

        // Create Stack of Integers
        Stack<Integer> stack = new Stack<>();

        // Push elements
        stack.push(10);
        stack.push(20);
        stack.push(30);

        // Display stack
        System.out.println("Stack elements: " + stack);

        // Pop element (LIFO)
        int popped = stack.pop();
        System.out.println("Popped element: " + popped);

        // Display stack after pop
        System.out.println("Stack after pop: " + stack);

        // Display elements using loop
        System.out.println("Displaying stack elements:");
        for (int num : stack) {
            System.out.println(num);
        }
    }
}


File Name
Copy code

StackDemo.java
▶️ Compile Command
Copy code
Bash
javac StackDemo.java
▶️ Run Command
Copy code
Bash
java StackDemo
🖥️ Output
Copy code

Stack elements: [10, 20, 30]
Popped element: 30
Stack after pop: [10, 20]
Displaying stack elements:
10
20
📝 Easy Explanation
🔹 Stack (LIFO)
Last In First Out
Last pushed element is removed first
🔹 Methods Used
Method
Work
push()
Add element
pop()
Remove top element
Loop
Display elements
🔑 Exam Points
Stack is in java.util
Stack extends Vector
Used in undo/redo, recursion, expression evaluation
